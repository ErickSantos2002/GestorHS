# Caixas (agrupamento físico de OS) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir agrupar várias OS (de quaisquer clientes) numa "caixa" física, com ciclo Pendente→Aberta→Finalizada, vínculo/desvínculo/movimentação de OS e abertura de OS já dentro da caixa.

**Architecture:** A tabela `caixas` e a coluna `ordens.caixa` já existem (vieram da migração). Adicionamos a FK `ordens.caixa → caixas.id`, o modelo SQLAlchemy `Caixa`, um helper puro de workflow de status, schemas, um router `/caixas` e o frontend (`app/caixas/`), reaproveitando os padrões das fases anteriores (`excluir_protegido`, `require_funcao`, `crudClient`/`apiJson`, `AbrirOSModal`, `Modal`, `Badge`).

**Tech Stack:** Backend FastAPI + SQLAlchemy + Alembic + pytest (SQLite). Frontend React + TypeScript + Vite + Tailwind + Vitest/RTL.

**Spec:** `docs/superpowers/specs/2026-06-05-caixas-design.md`

**Convenções do projeto (importantes):**
- Testes backend rodam em SQLite via `tests/conftest.py` (PRAGMA foreign_keys=ON já habilitado). Models registram tabelas no metadata — por isso o teste do FK funciona.
- `require_funcao("Expedição", "Administrador")` para escrita; `get_current_usuario` para leitura.
- Rodar testes backend: `cd backend && python -m pytest -q` (ou via docker: `docker compose exec backend python -m pytest -q`).
- Rodar testes frontend: `cd frontend && npx vitest run`.
- Login de teste backend: helper `_headers(client, login, senha)` (ver tasks). Fixtures: `usuario_admin` (admin/senha123), `usuario_comum` (Expedição, comum/senha123), `usuario_lab` (lab/senha123), `fases_seed`, `os_base`.

---

## Task 1: Modelo `Caixa` + FK em `Ordem`

**Files:**
- Create: `backend/app/models/caixa.py`
- Modify: `backend/app/models/__init__.py`
- Modify: `backend/app/models/ordem.py:14` (coluna `caixa`) e relationships
- Test: `backend/tests/test_caixas_model.py`

- [ ] **Step 1: Write the failing test**

Create `backend/tests/test_caixas_model.py`:

```python
from datetime import date


def _seed_caixa_com_os(db_session):
    from app.models import Cliente, Equipamento, EquipamentoCliente, Caixa, Ordem
    cli_a = Cliente(nome="Cliente A")
    cli_b = Cliente(nome="Cliente B")
    eq = Equipamento(descricao="Bafômetro")
    db_session.add_all([cli_a, cli_b, eq]); db_session.flush()
    cx = Caixa(data=date(2026, 6, 5), status="P", obs="Lote Cuiabá")
    db_session.add(cx); db_session.flush()
    o1 = Ordem(cliente=cli_a.id, fase=4, caixa=cx.id, situacao="E")
    o2 = Ordem(cliente=cli_b.id, fase=4, caixa=cx.id, situacao="E")
    o3 = Ordem(cliente=cli_a.id, fase=4, caixa=cx.id, situacao="E")  # cliente A repetido
    db_session.add_all([o1, o2, o3]); db_session.commit(); db_session.refresh(cx)
    return cx


def test_caixa_total_os(db_session):
    cx = _seed_caixa_com_os(db_session)
    assert cx.total_os == 3


def test_caixa_clientes_distintos(db_session):
    cx = _seed_caixa_com_os(db_session)
    assert sorted(cx.clientes) == ["Cliente A", "Cliente B"]


def test_ordem_caixa_rel(db_session):
    cx = _seed_caixa_com_os(db_session)
    o = cx.ordens[0]
    assert o.caixa_rel is not None
    assert o.caixa_obs == "Lote Cuiabá"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_caixas_model.py -q`
Expected: FAIL (`ImportError: cannot import name 'Caixa'`).

- [ ] **Step 3: Create the model**

Create `backend/app/models/caixa.py`:

```python
from sqlalchemy import Column, Integer, String, Date
from sqlalchemy.orm import relationship
from app.models.database import Base


class Caixa(Base):
    __tablename__ = "caixas"

    id = Column(Integer, primary_key=True, index=True)
    data = Column(Date, nullable=True)
    status = Column(String(1), nullable=False, default="P")  # P=Pendente, A=Aberta, F=Finalizada
    obs = Column(String(1000), nullable=True)

    ordens = relationship("Ordem", back_populates="caixa_rel", lazy="selectin")

    @property
    def total_os(self) -> int:
        return len(self.ordens)

    @property
    def clientes(self) -> list[str]:
        nomes = {o.cliente_nome for o in self.ordens if o.cliente_nome}
        return sorted(nomes)
```

- [ ] **Step 4: Wire the FK + relationship in `Ordem`**

In `backend/app/models/ordem.py`, change line 14 from:

```python
    caixa = Column(Integer, nullable=True)
```

to:

```python
    caixa = Column(Integer, ForeignKey("caixas.id"), nullable=True)
```

(`ForeignKey` já está importado na linha 1.) E, junto aos outros relationships (após a linha `fase_rel = relationship(...)`, ~linha 62), adicione:

```python
    caixa_rel = relationship("Caixa", back_populates="ordens", lazy="joined")
```

E adicione a property (junto às outras `@property`):

```python
    @property
    def caixa_obs(self):
        return self.caixa_rel.obs if self.caixa_rel else None
```

- [ ] **Step 5: Register the model**

In `backend/app/models/__init__.py`, adicione o import (após a linha `from app.models.foto import Foto`):

```python
from app.models.caixa import Caixa
```

e inclua `"Caixa"` no `__all__`.

- [ ] **Step 6: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_caixas_model.py -q`
Expected: PASS (3 passed).

- [ ] **Step 7: Run full suite (no regressions)**

Run: `cd backend && python -m pytest -q`
Expected: all green (baseline 199 + 3 novos).

- [ ] **Step 8: Commit**

```bash
git add backend/app/models/caixa.py backend/app/models/__init__.py backend/app/models/ordem.py backend/tests/test_caixas_model.py
git commit -m "feat(caixas): modelo Caixa + FK ordens.caixa→caixas"
```

---

## Task 2: Helper de workflow de status `caixas_workflow`

**Files:**
- Create: `backend/app/core/caixas_workflow.py`
- Test: `backend/tests/test_caixas_workflow.py`

- [ ] **Step 1: Write the failing test**

Create `backend/tests/test_caixas_workflow.py`:

```python
import pytest
from app.core import caixas_workflow as cw


def test_constantes():
    assert (cw.PENDENTE, cw.ABERTA, cw.FINALIZADA) == ("P", "A", "F")


def test_pode_vincular():
    assert cw.pode_vincular("P") is True
    assert cw.pode_vincular("A") is True
    assert cw.pode_vincular("F") is False


def test_proximo_status_valido():
    assert cw.proximo_status("P") == "A"
    assert cw.proximo_status("A") == "F"


def test_validar_transicao_ok():
    cw.validar_transicao("P", "A")   # não levanta
    cw.validar_transicao("A", "F")   # não levanta


def test_validar_transicao_invalida():
    with pytest.raises(ValueError):
        cw.validar_transicao("P", "F")
    with pytest.raises(ValueError):
        cw.validar_transicao("F", "A")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_caixas_workflow.py -q`
Expected: FAIL (módulo inexistente).

- [ ] **Step 3: Implement the helper**

Create `backend/app/core/caixas_workflow.py`:

```python
"""Workflow puro de status da caixa: Pendente (P) → Aberta (A) → Finalizada (F)."""

PENDENTE = "P"
ABERTA = "A"
FINALIZADA = "F"

# transições lineares válidas (status atual → próximo permitido)
TRANSICOES = {PENDENTE: ABERTA, ABERTA: FINALIZADA}


def pode_vincular(status: str) -> bool:
    """Só dá para vincular/abrir OS enquanto a caixa não está Finalizada."""
    return status in (PENDENTE, ABERTA)


def proximo_status(atual: str) -> str | None:
    return TRANSICOES.get(atual)


def validar_transicao(atual: str, novo: str) -> None:
    """Levanta ValueError se a transição não for a próxima permitida."""
    if TRANSICOES.get(atual) != novo:
        raise ValueError(f"transição inválida: {atual} → {novo}")
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_caixas_workflow.py -q`
Expected: PASS (5 passed).

- [ ] **Step 5: Commit**

```bash
git add backend/app/core/caixas_workflow.py backend/tests/test_caixas_workflow.py
git commit -m "feat(caixas): helper puro de workflow de status (P→A→F)"
```

---

## Task 3: Schemas das caixas

**Files:**
- Create: `backend/app/schemas/caixas.py`
- Test: `backend/tests/test_caixas_schemas.py`

- [ ] **Step 1: Write the failing test**

Create `backend/tests/test_caixas_schemas.py`:

```python
from datetime import date


def test_caixa_out_from_model(db_session):
    from app.models import Cliente, Ordem, Caixa
    from app.schemas.caixas import CaixaOut, CaixaDetalhe
    cli = Cliente(nome="Cliente Z")
    db_session.add(cli); db_session.flush()
    cx = Caixa(data=date(2026, 6, 5), status="P", obs="origem X")
    db_session.add(cx); db_session.flush()
    db_session.add(Ordem(cliente=cli.id, fase=4, caixa=cx.id, situacao="E"))
    db_session.commit(); db_session.refresh(cx)

    out = CaixaOut.model_validate(cx)
    assert out.id == cx.id
    assert out.status == "P"
    assert out.total_os == 1
    assert out.clientes == ["Cliente Z"]

    det = CaixaDetalhe.model_validate(cx)
    assert len(det.ordens) == 1
    assert det.ordens[0].cliente_nome == "Cliente Z"


def test_vincular_ordem_in():
    from app.schemas.caixas import VincularOrdemIn
    assert VincularOrdemIn(ordem_id=7).ordem_id == 7
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_caixas_schemas.py -q`
Expected: FAIL (módulo inexistente).

- [ ] **Step 3: Implement the schemas**

Create `backend/app/schemas/caixas.py`:

```python
from datetime import date
from pydantic import BaseModel, ConfigDict


class CaixaCreate(BaseModel):
    obs: str | None = None


class CaixaUpdate(BaseModel):
    obs: str | None = None


class OrdemResumoCaixa(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: int
    cliente: int
    cliente_nome: str | None = None
    equipamento_descricao: str | None = None
    equipamento_serie: str | None = None
    fase: int | None = None
    fase_descricao: str | None = None
    fase_cor: str | None = None


class CaixaOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: int
    data: date | None = None
    status: str
    obs: str | None = None
    total_os: int = 0
    clientes: list[str] = []


class CaixaDetalhe(CaixaOut):
    ordens: list[OrdemResumoCaixa] = []


class CaixaPage(BaseModel):
    items: list[CaixaOut]
    total: int


class VincularOrdemIn(BaseModel):
    ordem_id: int
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_caixas_schemas.py -q`
Expected: PASS (2 passed).

- [ ] **Step 5: Commit**

```bash
git add backend/app/schemas/caixas.py backend/tests/test_caixas_schemas.py
git commit -m "feat(caixas): schemas (Create/Update/Out/Detalhe/Page/Vincular)"
```

---

## Task 4: Router de caixas — CRUD + transições de status

**Files:**
- Create: `backend/app/api/caixas.py`
- Modify: `backend/app/main.py:4,37` (import + include_router)
- Test: `backend/tests/test_caixas_crud.py`

- [ ] **Step 1: Write the failing test**

Create `backend/tests/test_caixas_crud.py`:

```python
def _headers(client, login, senha):
    tok = client.post("/auth/login", json={"login": login, "senha": senha}).json()
    return {"Authorization": f"Bearer {tok['access_token']}"}


def test_criar_caixa_nasce_pendente(client, usuario_comum, fases_seed):
    h = _headers(client, "comum", "senha123")
    r = client.post("/caixas", json={"obs": "Lote Cuiabá"}, headers=h)
    assert r.status_code == 201
    body = r.json()
    assert body["status"] == "P"
    assert body["obs"] == "Lote Cuiabá"
    assert body["data"] is not None
    assert body["total_os"] == 0


def test_listar_e_obter(client, usuario_comum):
    h = _headers(client, "comum", "senha123")
    client.post("/caixas", json={"obs": "A"}, headers=h)
    client.post("/caixas", json={"obs": "B"}, headers=h)
    lista = client.get("/caixas", headers=h).json()
    assert lista["total"] == 2
    cid = lista["items"][0]["id"]
    det = client.get(f"/caixas/{cid}", headers=h)
    assert det.status_code == 200
    assert "ordens" in det.json()


def test_busca_por_obs(client, usuario_comum):
    h = _headers(client, "comum", "senha123")
    client.post("/caixas", json={"obs": "Votorantim"}, headers=h)
    client.post("/caixas", json={"obs": "Outra"}, headers=h)
    r = client.get("/caixas?q=votor", headers=h).json()
    assert r["total"] == 1


def test_patch_obs(client, usuario_comum):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={"obs": "x"}, headers=h).json()["id"]
    r = client.patch(f"/caixas/{cid}", json={"obs": "novo"}, headers=h)
    assert r.status_code == 200
    assert r.json()["obs"] == "novo"


def test_transicoes_status(client, usuario_comum):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    assert client.post(f"/caixas/{cid}/abrir", headers=h).json()["status"] == "A"
    assert client.post(f"/caixas/{cid}/finalizar", headers=h).json()["status"] == "F"


def test_transicao_invalida_409(client, usuario_comum):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    # finalizar sem abrir → 409
    assert client.post(f"/caixas/{cid}/finalizar", headers=h).status_code == 409


def test_delete_caixa_vazia(client, usuario_comum):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    assert client.delete(f"/caixas/{cid}", headers=h).status_code == 204


def test_escrita_exige_expedicao_ou_admin(client, usuario_admin, usuario_lab):
    h = _headers(client, "lab", "senha123")
    assert client.post("/caixas", json={}, headers=h).status_code == 403


def test_leitura_qualquer_interno(client, usuario_lab):
    h = _headers(client, "lab", "senha123")
    assert client.get("/caixas", headers=h).status_code == 200
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_caixas_crud.py -q`
Expected: FAIL (rota /caixas inexistente → 404).

- [ ] **Step 3: Implement the router (CRUD + transições)**

Create `backend/app/api/caixas.py`:

```python
from datetime import date

from fastapi import APIRouter, Depends, HTTPException, status as http_status, Query
from sqlalchemy.orm import Session

from app.models.database import get_db
from app.models import Usuario, Caixa
from app.api.deps import get_current_usuario, require_funcao
from app.api.cadastros_common import excluir_protegido
from app.core import caixas_workflow as cw
from app.schemas.caixas import (
    CaixaCreate, CaixaUpdate, CaixaOut, CaixaDetalhe, CaixaPage,
)

router = APIRouter(prefix="/caixas", tags=["caixas"])

_escrita = require_funcao("Expedição", "Administrador")


def _get_caixa(db: Session, caixa_id: int) -> Caixa:
    cx = db.query(Caixa).filter(Caixa.id == caixa_id).first()
    if cx is None:
        raise HTTPException(status_code=404, detail="caixa não encontrada")
    return cx


@router.get("", response_model=CaixaPage)
def listar(
    status: str | None = None,
    q: str | None = None,
    offset: int = 0,
    limit: int = Query(25, ge=1, le=100),
    db: Session = Depends(get_db),
    _: Usuario = Depends(get_current_usuario),
):
    query = db.query(Caixa)
    if status:
        query = query.filter(Caixa.status == status)
    if q:
        if q.strip().isdigit():
            query = query.filter(Caixa.id == int(q.strip()))
        else:
            query = query.filter(Caixa.obs.ilike(f"%{q.strip()}%"))
    total = query.count()
    items = query.order_by(Caixa.id.desc()).offset(offset).limit(limit).all()
    return CaixaPage(items=[CaixaOut.model_validate(c) for c in items], total=total)


@router.get("/{caixa_id}", response_model=CaixaDetalhe)
def obter(caixa_id: int, db: Session = Depends(get_db), _: Usuario = Depends(get_current_usuario)):
    return _get_caixa(db, caixa_id)


@router.post("", response_model=CaixaOut, status_code=http_status.HTTP_201_CREATED)
def criar(dados: CaixaCreate, db: Session = Depends(get_db), _: Usuario = Depends(_escrita)):
    cx = Caixa(data=date.today(), status=cw.PENDENTE, obs=dados.obs)
    db.add(cx)
    db.commit()
    db.refresh(cx)
    return cx


@router.patch("/{caixa_id}", response_model=CaixaOut)
def atualizar(caixa_id: int, dados: CaixaUpdate, db: Session = Depends(get_db), _: Usuario = Depends(_escrita)):
    cx = _get_caixa(db, caixa_id)
    cx.obs = dados.obs
    db.commit()
    db.refresh(cx)
    return cx


def _transicionar(db: Session, cx: Caixa, novo: str) -> Caixa:
    try:
        cw.validar_transicao(cx.status, novo)
    except ValueError as e:
        raise HTTPException(status_code=409, detail=str(e))
    cx.status = novo
    db.commit()
    db.refresh(cx)
    return cx


@router.post("/{caixa_id}/abrir", response_model=CaixaOut)
def abrir_caixa(caixa_id: int, db: Session = Depends(get_db), _: Usuario = Depends(_escrita)):
    return _transicionar(db, _get_caixa(db, caixa_id), cw.ABERTA)


@router.post("/{caixa_id}/finalizar", response_model=CaixaOut)
def finalizar_caixa(caixa_id: int, db: Session = Depends(get_db), _: Usuario = Depends(_escrita)):
    return _transicionar(db, _get_caixa(db, caixa_id), cw.FINALIZADA)


@router.delete("/{caixa_id}", status_code=http_status.HTTP_204_NO_CONTENT)
def excluir(caixa_id: int, db: Session = Depends(get_db), _: Usuario = Depends(_escrita)):
    cx = _get_caixa(db, caixa_id)
    if cx.ordens:
        raise HTTPException(status_code=409, detail="caixa possui OS vinculadas")
    excluir_protegido(db, cx)
```

> Nota: bloqueamos delete com OS explicitamente (`cx.ordens`) porque o FK usa `ON DELETE SET NULL` — sem essa checagem, apagar a caixa soltaria as OS silenciosamente em vez de dar 409.

- [ ] **Step 4: Register the router**

In `backend/app/main.py`:
- Linha 4: adicione `caixas` à lista de imports (`from app.api import ... certificados, caixas`).
- Após a linha 37 (`app.include_router(certificados.router)`): adicione `app.include_router(caixas.router)`.

- [ ] **Step 5: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_caixas_crud.py -q`
Expected: PASS (9 passed).

- [ ] **Step 6: Commit**

```bash
git add backend/app/api/caixas.py backend/app/main.py backend/tests/test_caixas_crud.py
git commit -m "feat(caixas): router CRUD + transições de status (abrir/finalizar)"
```

---

## Task 5: Vincular / mover / desvincular OS na caixa

**Files:**
- Modify: `backend/app/api/caixas.py` (novos endpoints)
- Test: `backend/tests/test_caixas_ordens.py`

- [ ] **Step 1: Write the failing test**

Create `backend/tests/test_caixas_ordens.py`:

```python
def _headers(client, login, senha):
    tok = client.post("/auth/login", json={"login": login, "senha": senha}).json()
    return {"Authorization": f"Bearer {tok['access_token']}"}


def _ordem_solta(db_session, nome="Cliente OS"):
    from app.models import Cliente, Ordem
    cli = Cliente(nome=nome)
    db_session.add(cli); db_session.flush()
    o = Ordem(cliente=cli.id, fase=4, situacao="E")
    db_session.add(o); db_session.commit(); db_session.refresh(o)
    return o.id


def test_vincular_ordem(client, usuario_comum, fases_seed, db_session):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    oid = _ordem_solta(db_session)
    r = client.post(f"/caixas/{cid}/ordens", json={"ordem_id": oid}, headers=h)
    assert r.status_code == 200
    det = client.get(f"/caixas/{cid}", headers=h).json()
    assert [o["id"] for o in det["ordens"]] == [oid]


def test_vincular_aceita_clientes_diferentes(client, usuario_comum, fases_seed, db_session):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    o1 = _ordem_solta(db_session, "Cliente A")
    o2 = _ordem_solta(db_session, "Cliente B")
    client.post(f"/caixas/{cid}/ordens", json={"ordem_id": o1}, headers=h)
    client.post(f"/caixas/{cid}/ordens", json={"ordem_id": o2}, headers=h)
    det = client.get(f"/caixas/{cid}", headers=h).json()
    assert det["total_os"] == 2
    assert sorted(det["clientes"]) == ["Cliente A", "Cliente B"]


def test_mover_ordem_entre_caixas(client, usuario_comum, fases_seed, db_session):
    h = _headers(client, "comum", "senha123")
    c1 = client.post("/caixas", json={}, headers=h).json()["id"]
    c2 = client.post("/caixas", json={}, headers=h).json()["id"]
    oid = _ordem_solta(db_session)
    client.post(f"/caixas/{c1}/ordens", json={"ordem_id": oid}, headers=h)
    # mover: vincular a c2
    client.post(f"/caixas/{c2}/ordens", json={"ordem_id": oid}, headers=h)
    assert client.get(f"/caixas/{c1}", headers=h).json()["total_os"] == 0
    assert client.get(f"/caixas/{c2}", headers=h).json()["total_os"] == 1


def test_desvincular_ordem(client, usuario_comum, fases_seed, db_session):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    oid = _ordem_solta(db_session)
    client.post(f"/caixas/{cid}/ordens", json={"ordem_id": oid}, headers=h)
    r = client.delete(f"/caixas/{cid}/ordens/{oid}", headers=h)
    assert r.status_code == 204
    assert client.get(f"/caixas/{cid}", headers=h).json()["total_os"] == 0


def test_vincular_em_caixa_finalizada_409(client, usuario_comum, fases_seed, db_session):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    client.post(f"/caixas/{cid}/abrir", headers=h)
    client.post(f"/caixas/{cid}/finalizar", headers=h)
    oid = _ordem_solta(db_session)
    assert client.post(f"/caixas/{cid}/ordens", json={"ordem_id": oid}, headers=h).status_code == 409


def test_vincular_ordem_inexistente_404(client, usuario_comum):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    assert client.post(f"/caixas/{cid}/ordens", json={"ordem_id": 9999}, headers=h).status_code == 404


def test_desvincular_ordem_de_outra_caixa_404(client, usuario_comum, fases_seed, db_session):
    h = _headers(client, "comum", "senha123")
    c1 = client.post("/caixas", json={}, headers=h).json()["id"]
    c2 = client.post("/caixas", json={}, headers=h).json()["id"]
    oid = _ordem_solta(db_session)
    client.post(f"/caixas/{c1}/ordens", json={"ordem_id": oid}, headers=h)
    # desvincular pela caixa errada
    assert client.delete(f"/caixas/{c2}/ordens/{oid}", headers=h).status_code == 404
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_caixas_ordens.py -q`
Expected: FAIL (rotas inexistentes → 405/404).

- [ ] **Step 3: Implement the endpoints**

Em `backend/app/api/caixas.py`, adicione os imports no topo:

```python
from app.models import Ordem
from app.api.ordens_acoes import registrar_log
from app.schemas.caixas import VincularOrdemIn
```

(junte `Ordem` ao import existente `from app.models import Usuario, Caixa`.)

E adicione os endpoints ao final do arquivo:

```python
@router.post("/{caixa_id}/ordens", response_model=CaixaDetalhe)
def vincular_ordem(
    caixa_id: int,
    dados: VincularOrdemIn,
    db: Session = Depends(get_db),
    usuario: Usuario = Depends(_escrita),
):
    cx = _get_caixa(db, caixa_id)
    if not cw.pode_vincular(cx.status):
        raise HTTPException(status_code=409, detail="caixa finalizada não aceita vínculos")
    ordem = db.query(Ordem).filter(Ordem.id == dados.ordem_id).first()
    if ordem is None:
        raise HTTPException(status_code=404, detail="OS não encontrada")
    ordem.caixa = cx.id  # vincula/move (sem checar cliente)
    registrar_log(db, ordem, usuario, f"OS vinculada à caixa #{cx.id}")
    db.commit()
    db.refresh(cx)
    return cx


@router.delete("/{caixa_id}/ordens/{ordem_id}", status_code=http_status.HTTP_204_NO_CONTENT)
def desvincular_ordem(
    caixa_id: int,
    ordem_id: int,
    db: Session = Depends(get_db),
    usuario: Usuario = Depends(_escrita),
):
    cx = _get_caixa(db, caixa_id)
    if not cw.pode_vincular(cx.status):
        raise HTTPException(status_code=409, detail="caixa finalizada não aceita alterações")
    ordem = db.query(Ordem).filter(Ordem.id == ordem_id, Ordem.caixa == cx.id).first()
    if ordem is None:
        raise HTTPException(status_code=404, detail="OS não está nesta caixa")
    ordem.caixa = None
    registrar_log(db, ordem, usuario, f"OS removida da caixa #{cx.id}")
    db.commit()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_caixas_ordens.py -q`
Expected: PASS (7 passed).

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/caixas.py backend/tests/test_caixas_ordens.py
git commit -m "feat(caixas): vincular/mover/desvincular OS (com log e trava de finalizada)"
```

---

## Task 6: Abrir OS já dentro da caixa

**Files:**
- Modify: `backend/app/schemas/ordens.py:69-73` (`OrdemAbrirIn`)
- Modify: `backend/app/schemas/ordens.py` (`OrdemOut` e `OrdemListOut` → expor `caixa`)
- Modify: `backend/app/api/ordens.py:82-111` (`abrir`)
- Test: `backend/tests/test_ordens_abrir.py` (novos testes)

- [ ] **Step 1: Write the failing test**

Adicione ao final de `backend/tests/test_ordens_abrir.py`:

```python
def test_abrir_os_com_caixa(client, usuario_comum, fases_seed, os_base):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={"obs": "lote"}, headers=h).json()["id"]
    r = client.post("/ordens", json={
        "equipamento_cliente": os_base["equipamento_cliente"],
        "tipo_servico": "C", "caixa": cid,
    }, headers=h)
    assert r.status_code == 201
    assert r.json()["caixa"] == cid
    det = client.get(f"/caixas/{cid}", headers=h).json()
    assert det["total_os"] == 1


def test_abrir_os_em_caixa_finalizada_409(client, usuario_comum, fases_seed, os_base):
    h = _headers(client, "comum", "senha123")
    cid = client.post("/caixas", json={}, headers=h).json()["id"]
    client.post(f"/caixas/{cid}/abrir", headers=h)
    client.post(f"/caixas/{cid}/finalizar", headers=h)
    r = client.post("/ordens", json={
        "equipamento_cliente": os_base["equipamento_cliente"],
        "tipo_servico": "C", "caixa": cid,
    }, headers=h)
    assert r.status_code == 409


def test_abrir_os_caixa_inexistente_404(client, usuario_comum, fases_seed, os_base):
    h = _headers(client, "comum", "senha123")
    r = client.post("/ordens", json={
        "equipamento_cliente": os_base["equipamento_cliente"],
        "tipo_servico": "C", "caixa": 9999,
    }, headers=h)
    assert r.status_code == 404
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_ordens_abrir.py -q`
Expected: FAIL (campo `caixa` ignorado → `caixa` no retorno é None / KeyError).

- [ ] **Step 3: Estender `OrdemAbrirIn` e expor `caixa` no output**

Em `backend/app/schemas/ordens.py`, `OrdemAbrirIn` (linhas 69-73) passa a:

```python
class OrdemAbrirIn(BaseModel):
    equipamento_cliente: int
    tipo_servico: Literal["C", "M", "A"]
    condicao_chegada: str | None = None
    acessorios: str | None = None
    caixa: int | None = None
```

E adicione `caixa: int | None = None` em `OrdemListOut` (após `situacao`, ~linha 20) e em `OrdemOut` (junto aos demais campos, ex.: após `obs`).

- [ ] **Step 4: Aplicar a caixa ao abrir**

Em `backend/app/api/ordens.py`, no topo, junte `Caixa` ao import dos models:

```python
from app.models import Usuario, Ordem, Cliente, Fase, LogOS, EquipamentoCliente, Caixa
```

E no corpo de `abrir`, **após** o bloco que valida `ec`/OS ativa e **antes** de criar `ordem` (antes da linha `ordem = Ordem(...)`), adicione a validação da caixa:

```python
    if dados.caixa is not None:
        cx = db.query(Caixa).filter(Caixa.id == dados.caixa).first()
        if cx is None:
            raise HTTPException(status_code=404, detail="caixa não encontrada")
        from app.core import caixas_workflow as cw
        if not cw.pode_vincular(cx.status):
            raise HTTPException(status_code=409, detail="caixa finalizada não aceita vínculos")
```

E no construtor `Ordem(...)` (linhas 94-104), adicione o campo:

```python
        caixa=dados.caixa,
```

- [ ] **Step 5: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_ordens_abrir.py -q`
Expected: PASS (todos, incl. 3 novos).

- [ ] **Step 6: Run full backend suite**

Run: `cd backend && python -m pytest -q`
Expected: tudo verde.

- [ ] **Step 7: Commit**

```bash
git add backend/app/schemas/ordens.py backend/app/api/ordens.py backend/tests/test_ordens_abrir.py
git commit -m "feat(caixas): abrir OS já vinculada a uma caixa + expor caixa no OrdemOut"
```

---

## Task 7: Migração Alembic `0004_caixas_fk`

**Files:**
- Create: `backend/alembic/versions/0004_caixas_fk.py`

> A suíte pytest usa SQLite/metadata e **não** roda Alembic. Esta migração é para o **banco real (9998)** e segue o protocolo do projeto: dry-run + aprovação do usuário antes de aplicar.

- [ ] **Step 1: Create the migration file**

Create `backend/alembic/versions/0004_caixas_fk.py`:

```python
"""caixas: FK ordens.caixa -> caixas.id

Revision ID: 0004_caixas_fk
Revises: 0003_solicitacoes
Create Date: 2026-06-05
"""
from alembic import op

revision = "0004_caixas_fk"
down_revision = "0003_solicitacoes"
branch_labels = None
depends_on = None


def upgrade():
    # Solta órfãos: OS apontando para caixa inexistente viraria violação de FK.
    op.execute(
        "UPDATE ordens SET caixa = NULL "
        "WHERE caixa IS NOT NULL AND caixa NOT IN (SELECT id FROM caixas)"
    )
    op.create_foreign_key(
        "fk_ordens_caixa", "ordens", "caixas",
        ["caixa"], ["id"], ondelete="SET NULL",
    )


def downgrade():
    op.drop_constraint("fk_ordens_caixa", "ordens", type_="foreignkey")
```

- [ ] **Step 2: Verify it imports (sanity)**

Run: `cd backend && python -c "import importlib.util,glob; [importlib.util.spec_from_file_location('m',f) for f in glob.glob('alembic/versions/0004_caixas_fk.py')]; print('ok')"`
Expected: `ok` (sem erro de sintaxe).

- [ ] **Step 3: Commit (sem aplicar ainda)**

```bash
git add backend/alembic/versions/0004_caixas_fk.py
git commit -m "feat(caixas): migração 0004 FK ordens.caixa→caixas (aplicar após aprovação)"
```

- [ ] **Step 4: Dry-run + aplicar no banco real (com o usuário)**

> **Não execute sem confirmar com o usuário.** Protocolo:
> - Dry-run (SQL offline): `cd backend && alembic upgrade 0003_solicitacoes:0004_caixas_fk --sql` (ou `alembic upgrade head --sql`) e mostrar o SQL.
> - Quantos órfãos seriam soltos? `SELECT count(*) FROM ordens WHERE caixa IS NOT NULL AND caixa NOT IN (SELECT id FROM caixas);`
> - Após o ok do usuário: `cd backend && alembic upgrade head` (ou `docker compose exec backend alembic upgrade head`).
> - Verificar round-trip do downgrade em ambiente seguro, se viável.

---

## Task 8: Frontend — `caixasApi` + testes

**Files:**
- Create: `frontend/src/app/caixas/api.ts`
- Test: `frontend/src/app/caixas/api.test.ts`

- [ ] **Step 1: Write the failing test**

Create `frontend/src/app/caixas/api.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

const apiJson = vi.fn()
vi.mock('../../lib/api', () => ({ apiJson: (...a: unknown[]) => apiJson(...a) }))

import { caixasApi } from './api'

beforeEach(() => { apiJson.mockReset(); apiJson.mockResolvedValue({}) })

describe('caixasApi', () => {
  it('listar monta query string com status e q', async () => {
    await caixasApi.listar({ status: 'P', q: 'votor', offset: 25, limit: 25 })
    expect(apiJson).toHaveBeenCalledWith('/caixas?status=P&q=votor&offset=25&limit=25')
  })

  it('criar manda POST com obs', async () => {
    await caixasApi.criar({ obs: 'lote' })
    expect(apiJson).toHaveBeenCalledWith('/caixas', { method: 'POST', body: JSON.stringify({ obs: 'lote' }) })
  })

  it('abrir e finalizar usam POST nas rotas certas', async () => {
    await caixasApi.abrir(3)
    expect(apiJson).toHaveBeenCalledWith('/caixas/3/abrir', { method: 'POST' })
    await caixasApi.finalizar(3)
    expect(apiJson).toHaveBeenCalledWith('/caixas/3/finalizar', { method: 'POST' })
  })

  it('vincular e desvincular OS', async () => {
    await caixasApi.vincularOrdem(3, 7)
    expect(apiJson).toHaveBeenCalledWith('/caixas/3/ordens', { method: 'POST', body: JSON.stringify({ ordem_id: 7 }) })
    await caixasApi.desvincularOrdem(3, 7)
    expect(apiJson).toHaveBeenCalledWith('/caixas/3/ordens/7', { method: 'DELETE' })
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npx vitest run src/app/caixas/api.test.ts`
Expected: FAIL (módulo inexistente).

- [ ] **Step 3: Implement `caixasApi`**

Create `frontend/src/app/caixas/api.ts`:

```ts
import { apiJson } from '../../lib/api'

export function formatData(iso: string | null): string {
  if (!iso) return '—'
  const d = new Date(iso)
  return isNaN(d.getTime()) ? '—' : d.toLocaleDateString('pt-BR')
}

export type StatusCaixa = 'P' | 'A' | 'F'

export const STATUS_CAIXA: Record<string, { label: string; tone: 'warning' | 'info' | 'success' }> = {
  P: { label: 'Pendente', tone: 'warning' },
  A: { label: 'Aberta', tone: 'info' },
  F: { label: 'Finalizada', tone: 'success' },
}

export interface OrdemResumoCaixa {
  id: number
  cliente: number
  cliente_nome: string | null
  equipamento_descricao: string | null
  equipamento_serie: string | null
  fase: number | null
  fase_descricao: string | null
  fase_cor: string | null
}

export interface CaixaListItem {
  id: number
  data: string | null
  status: StatusCaixa
  obs: string | null
  total_os: number
  clientes: string[]
}

export interface CaixaPage { items: CaixaListItem[]; total: number }

export interface CaixaDetalhe extends CaixaListItem {
  ordens: OrdemResumoCaixa[]
}

export interface CaixasParams { status?: string; q?: string; offset?: number; limit?: number }

export const caixasApi = {
  listar: (params: CaixasParams = {}): Promise<CaixaPage> => {
    const sp = new URLSearchParams()
    if (params.status) sp.set('status', params.status)
    if (params.q) sp.set('q', params.q)
    sp.set('offset', String(params.offset ?? 0))
    sp.set('limit', String(params.limit ?? 25))
    return apiJson<CaixaPage>(`/caixas?${sp.toString()}`)
  },
  obter: (id: number): Promise<CaixaDetalhe> => apiJson<CaixaDetalhe>(`/caixas/${id}`),
  criar: (body: { obs?: string | null }): Promise<CaixaListItem> =>
    apiJson<CaixaListItem>('/caixas', { method: 'POST', body: JSON.stringify(body) }),
  atualizar: (id: number, body: { obs?: string | null }): Promise<CaixaListItem> =>
    apiJson<CaixaListItem>(`/caixas/${id}`, { method: 'PATCH', body: JSON.stringify(body) }),
  abrir: (id: number): Promise<CaixaListItem> =>
    apiJson<CaixaListItem>(`/caixas/${id}/abrir`, { method: 'POST' }),
  finalizar: (id: number): Promise<CaixaListItem> =>
    apiJson<CaixaListItem>(`/caixas/${id}/finalizar`, { method: 'POST' }),
  excluir: (id: number): Promise<void> =>
    apiJson<void>(`/caixas/${id}`, { method: 'DELETE' }),
  vincularOrdem: (id: number, ordem_id: number): Promise<CaixaDetalhe> =>
    apiJson<CaixaDetalhe>(`/caixas/${id}/ordens`, { method: 'POST', body: JSON.stringify({ ordem_id }) }),
  desvincularOrdem: (id: number, ordem_id: number): Promise<void> =>
    apiJson<void>(`/caixas/${id}/ordens/${ordem_id}`, { method: 'DELETE' }),
}
```

> Confirme que `apiJson` aceita uma 2ª opção `RequestInit` e serializa JSON (igual ao uso em `ordens/api.ts`). Para `DELETE` que retorna 204, siga o que os outros módulos fazem (ex.: `usuariosPortalApi.excluir`); se `apiJson` quebrar em corpo vazio, use o mesmo utilitário deles.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npx vitest run src/app/caixas/api.test.ts`
Expected: PASS (4 passed).

- [ ] **Step 5: Commit**

```bash
git add frontend/src/app/caixas/api.ts frontend/src/app/caixas/api.test.ts
git commit -m "feat(caixas): caixasApi (front) + testes de query/payload"
```

---

## Task 9: Frontend — nav, ícone, rota e `CaixasPage` (lista + nova caixa)

**Files:**
- Modify: `frontend/src/components/ui/icons.tsx` (novo `IconCaixas`)
- Modify: `frontend/src/layout/Sidebar.tsx:6,17-26` (import + item de nav)
- Modify: `frontend/src/app/routes.tsx` (import + rotas)
- Create: `frontend/src/app/caixas/CaixasPage.tsx`

- [ ] **Step 1: Add the icon**

Em `frontend/src/components/ui/icons.tsx`, adicione (seguindo o estilo dos outros ícones — `stroke="currentColor"`, `className` default):

```tsx
export function IconCaixas({ className }: IconProps) {
  return (
    <svg className={className ?? 'w-5 h-5'} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <path d="M21 8 12 3 3 8l9 5 9-5Z" />
      <path d="M3 8v8l9 5 9-5V8" />
      <path d="M12 13v8" />
    </svg>
  )
}
```

- [ ] **Step 2: Add nav item**

Em `frontend/src/layout/Sidebar.tsx`:
- Linha 6: inclua `IconCaixas` no import dos ícones.
- No array `NAV_ITEMS` (após o item `Ordens`): adicione

```tsx
  { label: 'Caixas', icon: <IconCaixas />, to: '/app/caixas' },
```

- [ ] **Step 3: Add routes**

Em `frontend/src/app/routes.tsx`:
- Import: `import { CaixasPage } from './caixas/CaixasPage'` e `import { CaixaDetailPage } from './caixas/CaixaDetailPage'`.
- Rotas (após as de `ordens`):

```tsx
        <Route path="caixas" element={<CaixasPage />} />
        <Route path="caixas/:id" element={<CaixaDetailPage />} />
```

> `CaixaDetailPage` é criada na Task 10. Para esta task compilar isolada, crie um stub mínimo em `frontend/src/app/caixas/CaixaDetailPage.tsx`:
> ```tsx
> export function CaixaDetailPage() { return null }
> ```
> (será substituído na Task 10).

- [ ] **Step 4: Create `CaixasPage`**

Create `frontend/src/app/caixas/CaixasPage.tsx`. Estrutura (seguir os padrões de `SolicitacoesPage`/`OrdensPage`: título, filtros, tabela, paginação, `Badge`, `Spinner`, `useNavigate`):

```tsx
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { caixasApi, formatData, STATUS_CAIXA, type CaixaListItem } from './api'
import { Badge } from '../../components/ui/Badge'
import { Spinner } from '../../components/ui/Spinner'
import { Modal } from '../../components/ui/Modal'
import { useAuth } from '../../auth/AuthContext'
import { podeAbrirOS } from '../../auth/roles'
import { ApiError } from '../../lib/api'

const PAGE = 25

export function CaixasPage() {
  const navigate = useNavigate()
  const { user } = useAuth()
  const podeEscrever = podeAbrirOS(user)
  const [status, setStatus] = useState('')
  const [q, setQ] = useState('')
  const [busca, setBusca] = useState('')
  const [offset, setOffset] = useState(0)
  const [dados, setDados] = useState<{ items: CaixaListItem[]; total: number } | null>(null)
  const [novaAberta, setNovaAberta] = useState(false)
  const [novaObs, setNovaObs] = useState('')
  const [salvando, setSalvando] = useState(false)
  const [erro, setErro] = useState('')

  useEffect(() => {
    let vivo = true
    caixasApi.listar({ status: status || undefined, q: busca || undefined, offset, limit: PAGE })
      .then((r) => { if (vivo) setDados(r) })
    return () => { vivo = false }
  }, [status, busca, offset])

  async function criar(e: React.FormEvent) {
    e.preventDefault()
    setSalvando(true); setErro('')
    try {
      const c = await caixasApi.criar({ obs: novaObs.trim() || null })
      navigate(`/app/caixas/${c.id}`)
    } catch (err) {
      setErro(err instanceof ApiError ? err.message : 'Falha ao criar caixa')
    } finally { setSalvando(false) }
  }

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-2xl font-bold text-slate-100">Caixas</h1>
          <p className="text-sm text-slate-500">Agrupamento físico de ordens de serviço.</p>
        </div>
        {podeEscrever && (
          <button onClick={() => { setNovaObs(''); setErro(''); setNovaAberta(true) }}
            className="px-4 py-2 rounded-lg bg-primary text-white text-sm font-semibold hover:bg-primary-600 transition-all">
            Nova caixa
          </button>
        )}
      </div>

      <div className="flex gap-3 flex-wrap">
        <select value={status} onChange={(e) => { setOffset(0); setStatus(e.target.value) }}
          className="px-3 py-2 text-sm rounded-lg border border-border bg-background-elevated text-slate-300">
          <option value="">Todos os status</option>
          <option value="P">Pendente</option>
          <option value="A">Aberta</option>
          <option value="F">Finalizada</option>
        </select>
        <form onSubmit={(e) => { e.preventDefault(); setOffset(0); setBusca(q) }} className="flex gap-2">
          <input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Buscar nº ou descrição"
            className="px-3 py-2 text-sm rounded-lg border border-border bg-background-elevated text-slate-300" />
          <button className="px-3 py-2 text-sm rounded-lg border border-border text-slate-300 hover:bg-background-elevated">Buscar</button>
        </form>
      </div>

      {dados === null ? <Spinner /> : (
        <div className="rounded-xl border border-border overflow-hidden">
          <table className="w-full text-sm">
            <thead className="bg-background-elevated text-slate-400">
              <tr>
                <th className="text-left px-4 py-2.5 font-semibold">Caixa</th>
                <th className="text-left px-4 py-2.5 font-semibold">Data</th>
                <th className="text-left px-4 py-2.5 font-semibold">Status</th>
                <th className="text-left px-4 py-2.5 font-semibold">OS</th>
                <th className="text-left px-4 py-2.5 font-semibold">Clientes</th>
                <th className="text-left px-4 py-2.5 font-semibold">Descrição</th>
              </tr>
            </thead>
            <tbody>
              {dados.items.map((c) => (
                <tr key={c.id} onClick={() => navigate(`/app/caixas/${c.id}`)}
                  className="border-t border-border hover:bg-background-elevated cursor-pointer">
                  <td className="px-4 py-2.5 font-semibold text-slate-200">#{c.id}</td>
                  <td className="px-4 py-2.5 text-slate-400">{formatData(c.data)}</td>
                  <td className="px-4 py-2.5"><Badge tone={STATUS_CAIXA[c.status]?.tone}>{STATUS_CAIXA[c.status]?.label ?? c.status}</Badge></td>
                  <td className="px-4 py-2.5 text-slate-400">{c.total_os}</td>
                  <td className="px-4 py-2.5 text-slate-400">{c.clientes.length === 0 ? '—' : c.clientes.length === 1 ? c.clientes[0] : `${c.clientes.length} clientes`}</td>
                  <td className="px-4 py-2.5 text-slate-400 truncate max-w-xs">{c.obs ?? '—'}</td>
                </tr>
              ))}
              {dados.items.length === 0 && (
                <tr><td colSpan={6} className="px-4 py-8 text-center text-slate-500">Nenhuma caixa.</td></tr>
              )}
            </tbody>
          </table>
        </div>
      )}

      {dados && dados.total > PAGE && (
        <div className="flex items-center justify-between text-sm text-slate-400">
          <span>{dados.total} caixas</span>
          <div className="flex gap-2">
            <button disabled={offset === 0} onClick={() => setOffset(Math.max(0, offset - PAGE))}
              className="px-3 py-1.5 rounded-lg border border-border disabled:opacity-40">Anterior</button>
            <button disabled={offset + PAGE >= dados.total} onClick={() => setOffset(offset + PAGE)}
              className="px-3 py-1.5 rounded-lg border border-border disabled:opacity-40">Próxima</button>
          </div>
        </div>
      )}

      {novaAberta && (
        <Modal open onClose={() => setNovaAberta(false)} title="Nova caixa"
          footer={<>
            <button type="button" onClick={() => setNovaAberta(false)} className="flex-1 py-2.5 rounded-lg border border-border text-sm font-medium text-slate-400 hover:bg-background-elevated">Cancelar</button>
            <button type="submit" form="form-nova-caixa" disabled={salvando} className="flex-1 py-2.5 rounded-lg bg-primary text-white text-sm font-semibold hover:bg-primary-600 disabled:opacity-50">Criar</button>
          </>}>
          <form id="form-nova-caixa" onSubmit={criar} className="space-y-4">
            <div>
              <label htmlFor="obs" className="block text-xs font-semibold text-slate-500 mb-1.5 uppercase tracking-wide">Descrição / origem (opcional)</label>
              <input id="obs" value={novaObs} onChange={(e) => setNovaObs(e.target.value)} placeholder="Ex.: Lote Cuiabá"
                className="w-full px-3 py-2 text-sm rounded-lg border border-border bg-background-elevated text-slate-300 focus:outline-none focus:ring-2 focus:ring-primary/50" />
            </div>
            {erro && <p className="text-sm text-danger">{erro}</p>}
          </form>
        </Modal>
      )}
    </div>
  )
}
```

> Verifique a API exata de `Badge` (prop `tone`) e `Modal` (props `open`/`onClose`/`title`/`footer`) em `components/ui/` e ajuste se divergir. Reaproveite classes utilitárias dos componentes já existentes.

- [ ] **Step 5: Typecheck + lint + build**

Run: `cd frontend && npx tsc -b --noEmit && npx eslint src/app/caixas src/layout/Sidebar.tsx src/components/ui/icons.tsx && npm run build`
Expected: tudo sem erro.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/ui/icons.tsx frontend/src/layout/Sidebar.tsx frontend/src/app/routes.tsx frontend/src/app/caixas/CaixasPage.tsx frontend/src/app/caixas/CaixaDetailPage.tsx
git commit -m "feat(caixas): nav + rota + CaixasPage (lista + nova caixa)"
```

---

## Task 10: Frontend — `CaixaDetailPage` (header, transições, OS, ações)

**Files:**
- Modify: `frontend/src/app/caixas/CaixaDetailPage.tsx` (substitui o stub)
- Modify: `frontend/src/app/ordens/AbrirOSModal.tsx` (prop `caixa` opcional)
- Modify: `frontend/src/app/ordens/api.ts:91-96` (`AbrirPayload` ganha `caixa?`)

- [ ] **Step 1: `AbrirPayload` aceita `caixa`**

Em `frontend/src/app/ordens/api.ts`, `AbrirPayload` passa a:

```ts
export interface AbrirPayload {
  equipamento_cliente: number
  tipo_servico: TipoServico
  condicao_chegada?: string | null
  acessorios?: string | null
  caixa?: number | null
}
```

- [ ] **Step 2: `AbrirOSModal` aceita `caixa` e callback opcional**

Em `frontend/src/app/ordens/AbrirOSModal.tsx`:
- Estenda as props:

```tsx
export function AbrirOSModal({ equipamentoClienteId, osAtual, onClose, caixa, onAberta }: {
  equipamentoClienteId: number
  osAtual: number | null
  onClose: () => void
  caixa?: number
  onAberta?: (osId: number) => void
}) {
```

- No `ordensApi.abrir({...})`, inclua `caixa`:

```tsx
      const os = await ordensApi.abrir({
        equipamento_cliente: equipamentoClienteId,
        tipo_servico: tipo,
        condicao_chegada: condicao.trim() || null,
        acessorios: acessorios.trim() || null,
        caixa: caixa ?? null,
      })
      if (onAberta) onAberta(os.id)
      else navigate(`/app/ordens/${os.id}`)
```

> Comportamento atual (Frota) não passa `caixa`/`onAberta` → segue navegando para a OS, intacto.

- [ ] **Step 3: Implement `CaixaDetailPage`**

Substitua `frontend/src/app/caixas/CaixaDetailPage.tsx`. Requisitos:
- Carrega `caixasApi.obter(id)`; mostra cabeçalho (nº, data, `Badge` de status, `obs` editável via `caixasApi.atualizar`).
- Botões de transição (gated por `podeAbrirOS` e status): **Abrir caixa** quando `status==='P'`; **Finalizar** quando `status==='A'`. Após ação, recarrega.
- Lista das OS (`ordens`): nº (link `/app/ordens/:id`), cliente, equipamento+série, `Badge` de fase (cor `fase_cor`); por linha (gated, desabilitado se `status==='F'`): **Remover** (`desvincularOrdem` → recarrega) e **Mover** (abre modal de seleção de caixa destino → `vincularOrdem(destino, osId)` → recarrega).
- Ações do lote (gated, escondidas/desabilitadas se `status==='F'`):
  - **Abrir OS**: abre um picker de aparelho (input de busca → `apiJson('/equipamentos-cliente?q=...')`, **sem** filtro de cliente) e ao escolher um aparelho, abre `AbrirOSModal` com `caixa={id}` e `onAberta={() => recarrega()}`.
  - **Vincular OS existente**: input numérico do nº da OS → `vincularOrdem(id, osId)` → recarrega (trata 404/409 com mensagem).

Implementação de referência (ajuste nomes de componentes conforme `components/ui/`):

```tsx
import { useEffect, useState, useCallback } from 'react'
import { useParams, Link } from 'react-router-dom'
import { caixasApi, formatData, STATUS_CAIXA, type CaixaDetalhe } from './api'
import { AbrirOSModal } from '../ordens/AbrirOSModal'
import { Badge } from '../../components/ui/Badge'
import { Spinner } from '../../components/ui/Spinner'
import { Modal } from '../../components/ui/Modal'
import { useAuth } from '../../auth/AuthContext'
import { podeAbrirOS } from '../../auth/roles'
import { apiJson, ApiError } from '../../lib/api'

interface AparelhoBusca { id: number; serie: string | null; cliente_nome: string | null; equipamento_descricao: string | null }

export function CaixaDetailPage() {
  const { id } = useParams()
  const caixaId = Number(id)
  const { user } = useAuth()
  const podeEscrever = podeAbrirOS(user)
  const [caixa, setCaixa] = useState<CaixaDetalhe | null>(null)
  const [erro, setErro] = useState('')

  const recarregar = useCallback(() => {
    caixasApi.obter(caixaId).then(setCaixa).catch(() => setErro('Falha ao carregar caixa'))
  }, [caixaId])
  useEffect(() => { recarregar() }, [recarregar])

  // estados de modais: picker de aparelho (abrir OS), abrir-os modal, mover, vincular
  const [picker, setPicker] = useState(false)
  const [termo, setTermo] = useState('')
  const [aparelhos, setAparelhos] = useState<AparelhoBusca[]>([])
  const [aparelhoSel, setAparelhoSel] = useState<AparelhoBusca | null>(null)
  const [moverOsId, setMoverOsId] = useState<number | null>(null)
  const [destino, setDestino] = useState('')
  const [vincularAberto, setVincularAberto] = useState(false)
  const [vincularOsId, setVincularOsId] = useState('')

  if (!caixa) return erro ? <p className="text-danger">{erro}</p> : <Spinner />
  const finalizada = caixa.status === 'F'

  async function buscarAparelhos(t: string) {
    setTermo(t)
    if (t.trim().length < 2) { setAparelhos([]); return }
    const r = await apiJson<{ items: AparelhoBusca[] }>(`/equipamentos-cliente?q=${encodeURIComponent(t.trim())}&limit=10`)
    setAparelhos(r.items)
  }

  async function transicao(fn: () => Promise<unknown>) {
    setErro('')
    try { await fn(); recarregar() }
    catch (err) { setErro(err instanceof ApiError ? err.message : 'Falha na operação') }
  }

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <div>
          <Link to="/app/caixas" className="text-xs text-primary hover:underline">← Caixas</Link>
          <h1 className="text-2xl font-bold text-slate-100">Caixa #{caixa.id}</h1>
          <p className="text-sm text-slate-500">{formatData(caixa.data)} · {caixa.total_os} OS · {caixa.clientes.length} cliente(s)</p>
        </div>
        <div className="flex items-center gap-2">
          <Badge tone={STATUS_CAIXA[caixa.status]?.tone}>{STATUS_CAIXA[caixa.status]?.label}</Badge>
          {podeEscrever && caixa.status === 'P' && (
            <button onClick={() => transicao(() => caixasApi.abrir(caixa.id))} className="px-3 py-1.5 rounded-lg bg-primary text-white text-sm font-semibold">Abrir caixa</button>
          )}
          {podeEscrever && caixa.status === 'A' && (
            <button onClick={() => transicao(() => caixasApi.finalizar(caixa.id))} className="px-3 py-1.5 rounded-lg bg-primary text-white text-sm font-semibold">Finalizar</button>
          )}
        </div>
      </div>

      {/* obs editável (apenas se podeEscrever e não finalizada) — input + botão Salvar via caixasApi.atualizar */}

      {podeEscrever && !finalizada && (
        <div className="flex gap-2">
          <button onClick={() => { setTermo(''); setAparelhos([]); setAparelhoSel(null); setPicker(true) }}
            className="px-3 py-2 rounded-lg border border-border text-sm text-slate-200 hover:bg-background-elevated">Abrir OS</button>
          <button onClick={() => { setVincularOsId(''); setVincularAberto(true) }}
            className="px-3 py-2 rounded-lg border border-border text-sm text-slate-200 hover:bg-background-elevated">Vincular OS existente</button>
        </div>
      )}
      {erro && <p className="text-sm text-danger">{erro}</p>}

      <div className="rounded-xl border border-border overflow-hidden">
        <table className="w-full text-sm">
          <thead className="bg-background-elevated text-slate-400">
            <tr>
              <th className="text-left px-4 py-2.5 font-semibold">OS</th>
              <th className="text-left px-4 py-2.5 font-semibold">Cliente</th>
              <th className="text-left px-4 py-2.5 font-semibold">Equipamento</th>
              <th className="text-left px-4 py-2.5 font-semibold">Fase</th>
              <th className="px-4 py-2.5"></th>
            </tr>
          </thead>
          <tbody>
            {caixa.ordens.map((o) => (
              <tr key={o.id} className="border-t border-border">
                <td className="px-4 py-2.5"><Link to={`/app/ordens/${o.id}`} className="font-semibold text-primary hover:underline">#{o.id}</Link></td>
                <td className="px-4 py-2.5 text-slate-300">{o.cliente_nome ?? '—'}</td>
                <td className="px-4 py-2.5 text-slate-400">{o.equipamento_descricao ?? '—'}{o.equipamento_serie ? ` · ${o.equipamento_serie}` : ''}</td>
                <td className="px-4 py-2.5">{o.fase_descricao && <Badge style={{ backgroundColor: `#${o.fase_cor}22`, color: `#${o.fase_cor}` }}>{o.fase_descricao}</Badge>}</td>
                <td className="px-4 py-2.5 text-right whitespace-nowrap">
                  {podeEscrever && !finalizada && (
                    <>
                      <button onClick={() => { setMoverOsId(o.id); setDestino('') }} className="text-xs text-slate-400 hover:text-slate-200 mr-3">Mover</button>
                      <button onClick={() => transicao(() => caixasApi.desvincularOrdem(caixa.id, o.id))} className="text-xs text-danger hover:underline">Remover</button>
                    </>
                  )}
                </td>
              </tr>
            ))}
            {caixa.ordens.length === 0 && <tr><td colSpan={5} className="px-4 py-8 text-center text-slate-500">Nenhuma OS nesta caixa.</td></tr>}
          </tbody>
        </table>
      </div>

      {/* Picker de aparelho → escolhe → AbrirOSModal */}
      {picker && !aparelhoSel && (
        <Modal open onClose={() => setPicker(false)} title="Abrir OS — escolher aparelho">
          <input autoFocus value={termo} onChange={(e) => buscarAparelhos(e.target.value)} placeholder="Buscar série / patrimônio / cliente"
            className="w-full px-3 py-2 text-sm rounded-lg border border-border bg-background-elevated text-slate-300 mb-3" />
          <div className="max-h-72 overflow-y-auto divide-y divide-border">
            {aparelhos.map((a) => (
              <button key={a.id} onClick={() => setAparelhoSel(a)} className="w-full text-left px-2 py-2 hover:bg-background-elevated text-sm">
                <span className="text-slate-200">{a.equipamento_descricao ?? 'Equipamento'}</span>
                <span className="text-slate-500"> · {a.serie ?? 's/ série'} · {a.cliente_nome ?? '—'}</span>
              </button>
            ))}
            {termo.trim().length >= 2 && aparelhos.length === 0 && <p className="text-sm text-slate-500 px-2 py-3">Nada encontrado.</p>}
          </div>
        </Modal>
      )}
      {picker && aparelhoSel && (
        <AbrirOSModal
          equipamentoClienteId={aparelhoSel.id}
          osAtual={null}
          caixa={caixa.id}
          onAberta={() => { setPicker(false); setAparelhoSel(null); recarregar() }}
          onClose={() => { setPicker(false); setAparelhoSel(null) }}
        />
      )}

      {/* Vincular OS existente */}
      {vincularAberto && (
        <Modal open onClose={() => setVincularAberto(false)} title="Vincular OS existente"
          footer={<>
            <button type="button" onClick={() => setVincularAberto(false)} className="flex-1 py-2.5 rounded-lg border border-border text-sm text-slate-400">Cancelar</button>
            <button type="button" onClick={() => transicao(async () => { await caixasApi.vincularOrdem(caixa.id, Number(vincularOsId)); setVincularAberto(false) })}
              className="flex-1 py-2.5 rounded-lg bg-primary text-white text-sm font-semibold">Vincular</button>
          </>}>
          <label className="block text-xs font-semibold text-slate-500 mb-1.5 uppercase tracking-wide">Número da OS</label>
          <input value={vincularOsId} onChange={(e) => setVincularOsId(e.target.value.replace(/\D/g, ''))} placeholder="Ex.: 1234"
            className="w-full px-3 py-2 text-sm rounded-lg border border-border bg-background-elevated text-slate-300" />
        </Modal>
      )}

      {/* Mover OS → caixa destino */}
      {moverOsId !== null && (
        <Modal open onClose={() => setMoverOsId(null)} title={`Mover OS #${moverOsId}`}
          footer={<>
            <button type="button" onClick={() => setMoverOsId(null)} className="flex-1 py-2.5 rounded-lg border border-border text-sm text-slate-400">Cancelar</button>
            <button type="button" onClick={() => transicao(async () => { await caixasApi.vincularOrdem(Number(destino), moverOsId!); setMoverOsId(null) })}
              className="flex-1 py-2.5 rounded-lg bg-primary text-white text-sm font-semibold">Mover</button>
          </>}>
          <label className="block text-xs font-semibold text-slate-500 mb-1.5 uppercase tracking-wide">Caixa destino (nº)</label>
          <input value={destino} onChange={(e) => setDestino(e.target.value.replace(/\D/g, ''))} placeholder="Ex.: 57"
            className="w-full px-3 py-2 text-sm rounded-lg border border-border bg-background-elevated text-slate-300" />
        </Modal>
      )}
    </div>
  )
}
```

> Confirme a API real de `Badge` (se aceita `style` para cor de fase — veja como `OrdemDetailPage`/`OrdensPage` renderizam o badge de fase e replique o mesmo padrão exato em vez do `style` inline acima). Reaproveite o mesmo componente/markup de badge de fase já usado nas telas de OS.

- [ ] **Step 4: Typecheck + lint + build**

Run: `cd frontend && npx tsc -b --noEmit && npx eslint src/app/caixas src/app/ordens/AbrirOSModal.tsx src/app/ordens/api.ts && npm run build`
Expected: sem erros.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/app/caixas/CaixaDetailPage.tsx frontend/src/app/ordens/AbrirOSModal.tsx frontend/src/app/ordens/api.ts
git commit -m "feat(caixas): CaixaDetailPage (transições, OS, abrir/vincular/mover/remover)"
```

---

## Task 11: Frontend — mostrar a caixa no detalhe da OS

**Files:**
- Modify: `frontend/src/app/ordens/api.ts:54-72` (`OrdemDetalhe` ganha `caixa`)
- Modify: `frontend/src/app/ordens/OrdemDetailPage.tsx` (bloco/linha "Caixa")

- [ ] **Step 1: Tipo `OrdemDetalhe` expõe `caixa`**

Em `frontend/src/app/ordens/api.ts`, dentro de `OrdemDetalhe` (e/ou `OrdemListItem` se preferir), adicione:

```ts
  caixa: number | null
```

(O backend já passou a expor `caixa` no `OrdemOut` na Task 6.)

- [ ] **Step 2: Mostrar no `OrdemDetailPage`**

Em `frontend/src/app/ordens/OrdemDetailPage.tsx`, no bloco de cabeçalho/dados de recebimento, acrescente uma linha condicional (seguindo o markup das demais linhas de dado da página):

```tsx
{ordem.caixa && (
  <p className="text-sm text-slate-400">
    Caixa: <Link to={`/app/caixas/${ordem.caixa}`} className="text-primary hover:underline">#{ordem.caixa}</Link>
  </p>
)}
```

> Garanta que `Link` está importado de `react-router-dom` no arquivo (provavelmente já está). Posicione a linha onde fizer sentido visual (ex.: junto à etiqueta/tipo de serviço).

- [ ] **Step 3: Typecheck + lint + build**

Run: `cd frontend && npx tsc -b --noEmit && npx eslint src/app/ordens && npm run build`
Expected: sem erros.

- [ ] **Step 4: Run frontend test suite**

Run: `cd frontend && npx vitest run`
Expected: tudo verde (incl. `caixas/api.test.ts`).

- [ ] **Step 5: Commit**

```bash
git add frontend/src/app/ordens/api.ts frontend/src/app/ordens/OrdemDetailPage.tsx
git commit -m "feat(caixas): detalhe da OS mostra a caixa vinculada"
```

---

## Task 12: Verificação E2E manual + checagem final

**Files:** nenhum (verificação).

- [ ] **Step 1: Garantir backend atualizado**

A imagem Docker precisa estar reconstruída (já feito hoje): `cd <raiz> && docker compose up -d --build`. Aplicar a migração 0004 no banco (Task 7, Step 4) **com aprovação do usuário** antes do E2E que cria caixas no banco real.

> Alternativa segura: rodar o E2E e remover os dados de teste ao final (padrão das fases anteriores).

- [ ] **Step 2: Rodar as suítes**

Run backend: `cd backend && python -m pytest -q` → tudo verde.
Run frontend: `cd frontend && npx vitest run` → tudo verde.
Run: `cd frontend && npx tsc -b --noEmit && npm run build` → ok.

- [ ] **Step 3: E2E no navegador (login admin)**

Com front (`npm run dev`, :5173) + back (:8000) no ar, percorrer:
1. Nav "Caixas" → **Nova caixa** com descrição "Lote teste" → cai no detalhe (status Pendente).
2. **Abrir OS** → buscar aparelho do **Cliente A** → abrir; repetir para um aparelho do **Cliente B** (CNPJ diferente). Conferir que o cabeçalho mostra "2 OS · 2 cliente(s)" e a lista traz os dois clientes.
3. Criar uma 2ª caixa; voltar à 1ª e **Mover** uma OS para a 2ª (informando o nº). Conferir contagens nas duas.
4. **Remover** a OS restante da 1ª caixa (desvincula).
5. **Vincular OS existente** informando o nº de uma OS solta.
6. **Abrir caixa** (P→A) e **Finalizar** (A→F); confirmar que os botões/ações de OS somem/desabilitam (Finalizada trava).
7. Tentar **excluir** uma caixa com OS → erro 409 (mensagem).
8. Abrir o **detalhe de uma OS** vinculada → ver o link "Caixa #N".

- [ ] **Step 4: Limpar dados de teste**

Remover caixas/OS de teste criadas (via DELETE da API onde permitido, ou direto no banco com cuidado), seguindo o padrão das fases anteriores.

- [ ] **Step 5: Atualizar memória do projeto**

Atualizar `project_gestorhs.md` (entrada de Caixas) de "em brainstorming" para "CONCLUÍDA", com contagem de testes e o resumo do que foi entregue.

---

## Self-Review (preenchido na escrita do plano)

**Cobertura da spec:**
- Migração só-FK → Task 7. Modelo + FK → Task 1. Workflow P→A→F → Task 2 + Task 4. Schemas → Task 3. CRUD/transições/delete-protegido → Task 4. Vincular/mover/desvincular sem trava de cliente + trava de finalizada + log → Task 5. Abrir OS com caixa + expor `caixa` no OrdemOut → Task 6. Frontend api → Task 8. Nav/rota/lista/nova caixa → Task 9. Detalhe + ações + AbrirOSModal → Task 10. Caixa no detalhe da OS → Task 11. Papéis (leitura interno / escrita Expedição-Admin) → cobertos nas Tasks 4 e 5 (testes 403/200). E2E + critérios de aceite → Task 12. ✓
- Itens deferidos (reabrir, etiqueta, portal, fechamento automático) permanecem fora — sem tasks, correto.

**Placeholders:** nenhum "TBD/TODO"; trechos de UI que dependem de componentes existentes têm nota explícita para conferir a API real de `Badge`/`Modal` e replicar o padrão (não é placeholder de lógica).

**Consistência de tipos/nomes:** `Caixa.total_os`/`clientes`; `caixas_workflow.{PENDENTE,ABERTA,FINALIZADA,pode_vincular,proximo_status,validar_transicao,TRANSICOES}`; schemas `CaixaCreate/Update/Out/Detalhe/Page`, `OrdemResumoCaixa`, `VincularOrdemIn`; rotas `/caixas`, `/caixas/{id}/abrir|finalizar|ordens`; front `caixasApi.{listar,obter,criar,atualizar,abrir,finalizar,excluir,vincularOrdem,desvincularOrdem}`, `STATUS_CAIXA`, tipos `CaixaListItem/CaixaPage/CaixaDetalhe/OrdemResumoCaixa`. Consistentes entre tasks. ✓
